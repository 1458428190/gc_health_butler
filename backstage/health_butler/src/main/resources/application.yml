spring:
  profiles:
    active: local
  application:
    name: health_butler

server:
  servlet:
    context-path: /health_butler
  port: 8086

logging:
  config: classpath:logback-spring.xml
  level:
    dao: debug
    org:
      mybatis: debug

